# line = '                  DECLARE J INT 0;'
# count = 0
# flag = False
# varN = 'J'
# print line.strip().split(' ')
# for word in line.strip().split(' '):
	# count += 1
	# print str(flag)+' '+word+' '+str(count)
	# if flag:
		# if (count == 4):
			# initVal = int(float(word.split(';')[0]))
			# if initVal == 0:
				# #print 'intial value problem in declaration at '+line.strip()
				# # l = line.strip().split(' ')
				# # if line.strip().startswith('D'):
					# # leadSpace = line.split('D')
				# # elif line.strip().startswith('d'):
					# # leadSpace = line.split('d')
				# # tmp = leadSpace[0] + l[0] + ' ' + l[1] + ' ' + l[2] + ' ' + str(1) + ';'
				# print 'Declaration statement intialized with 0 for Index variable was found.\n'
		
	# if count == 2 and word==varN:
		# #print '\t\t'+line.strip()
		# flag = True
x = raw_input('Enter:')
print x